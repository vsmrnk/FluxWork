# FluxWork environment variables. Copy to .env.local and fill in.
# Never commit real secrets.
#
# There are two environments — see docs/ENVIRONMENTS.md. Locally you want the
# *dev* Supabase project, never prod: whatever NEXT_PUBLIC_SUPABASE_URL points
# at is what `npm run dev` reads and writes.

# ── Supabase ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service-role key — SERVER ONLY, bypasses RLS. Used by the Paddle webhook to
# write subscription state. Never expose to the browser (no NEXT_PUBLIC_ prefix).
SUPABASE_SERVICE_ROLE_KEY=

# ── Paddle Billing (Epic E) ─────────────────────────────────────────────────
# Client-side (safe to expose) — power the overlay checkout.
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=
NEXT_PUBLIC_PADDLE_PRICE_ID=
NEXT_PUBLIC_PADDLE_ENV=sandbox            # 'sandbox' | 'production'

# Optional: hosted customer-portal URL shown to paid users on /billing.
NEXT_PUBLIC_PADDLE_CUSTOMER_PORTAL_URL=

# Server-side Paddle API key (pdl_sdbx_… sandbox / pdl_live_… live). Used only by
# `npm run paddle:setup` to provision the product/price. Not read at runtime.
PADDLE_API_KEY=

# Server-side — webhook signature verification (Paddle dashboard → Notifications).
PADDLE_WEBHOOK_SECRET=

# Invoice PDF + DOCX are generated natively in the Next.js backend
# (src/lib/invoicePdf.tsx via @react-pdf/renderer, src/lib/docx.ts). No external
# conversion service is required.

# ── Auth transactional email (Supabase Send Email hook → Next.js → Resend) ──
# Supabase's "Send Email" auth hook POSTs to /api/auth/send-email. Set the hook
# target to <app-origin>/api/auth/send-email and paste its signing secret here.
AUTH_EMAIL_HOOK_SECRET=            # v1,whsec_… from Supabase → Auth → Hooks → Send Email
# Resend delivery (https://resend.com → API Keys; sender domain must be verified).
RESEND_API_KEY=
AUTH_EMAIL_FROM=FluxWork <noreply@yourdomain.com>
# App origin used to build the /auth/confirm link (do NOT rely on Supabase's
# Site URL for this). Should be the production app URL. Also used by the weekly
# digest for its "Open FluxWork" link.
AUTH_EMAIL_SITE_URL=https://fluxwork-gamma.vercel.app

# ── Weekly email digest (Vercel Cron → /api/digest) ─────────────────────────
# Shared secret the cron sends as `Authorization: Bearer <CRON_SECRET>`; the
# route rejects any other value. Vercel injects this into scheduled invocations.
CRON_SECRET=
